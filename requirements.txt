# ============================================================
# Dentiligence — Python Dependencies
# ============================================================
# Install: pip install -r requirements.txt
# For GPU: ensure CUDA-compatible torch is installed first.
#   torch>=2.2.0 — Implicit Neural Representations, ray marching
# ============================================================

# Deep learning core
torch>=2.2.0
torchvision>=0.17.0

# Computer vision utilities (differentiable ops, image metrics)
kornia>=0.7.2

# API framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# Scientific computing
numpy>=1.26.0
scipy>=1.13.0

# Medical imaging (DICOM I/O)
pydicom>=2.4.4

# Mesh generation from neural fields (marching cubes)
scikit-image>=0.23.0

# Visualisation (optional, for development)
matplotlib>=3.8.0

# Testing
pytest>=8.2.0
httpx>=0.27.0   # ASGI test client for FastAPI
